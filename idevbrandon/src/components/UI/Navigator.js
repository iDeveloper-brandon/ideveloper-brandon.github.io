import React from "react";
import styled from "styled-components";

const Nav = styled.header`
  width: 100%;
  background-color: black;
`;

const Navigator = () => {
  return <Nav>iDevBrandon</Nav>;
};

export default Navigator;
